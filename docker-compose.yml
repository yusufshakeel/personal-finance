x-redash-service: &redash-service
  image: redash/redash:25.8.0
  depends_on:
    - postgres
    - redis
  env_file: .env
  restart: always

services:
  server:
    <<: *redash-service
    command: server
    ports:
      - "9000:5000"
    environment:
      REDASH_WEB_WORKERS: 1

  scheduler:
    <<: *redash-service
    command: scheduler
    depends_on:
      - server

  scheduled_worker:
    <<: *redash-service
    command: worker
    depends_on:
      - server
    environment:
      QUEUES: "scheduled_queries,schemas"
      WORKERS_COUNT: 1

  adhoc_worker:
    <<: *redash-service
    command: worker
    depends_on:
      - server
    environment:
      QUEUES: "queries"
      WORKERS_COUNT: 1

  worker:
    <<: *redash-service
    command: worker
    environment:
      QUEUES: "periodic,emails,default"
      WORKERS_COUNT: 1

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d

  mathesar:
    image: mathesar/mathesar:0.9.0
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/mathesar
    restart: unless-stopped

#  directus:
#    image: directus/directus:11
#    depends_on:
#      - postgres
#    ports:
#      - "8055:8055"
#    environment:
#      KEY: "randomkey"
#      SECRET: "supersecret"
#
#      DB_CLIENT: "pg"
#      DB_HOST: "host.docker.internal"
#      DB_PORT: "5432"
#      DB_DATABASE: "personal_finance"
#      DB_USER: "postgres"
#      DB_PASSWORD: "postgres"
#
#      ADMIN_EMAIL: "admin@example.com"
#      ADMIN_PASSWORD: "root1234"

  appsmith:
    image: appsmith/appsmith-ce:v1.97
    restart: unless-stopped
    ports:
      - "9002:80"
    volumes:
      - ./appsmith:/appsmith-stacks

volumes:
  postgres_data:
  redis_data: